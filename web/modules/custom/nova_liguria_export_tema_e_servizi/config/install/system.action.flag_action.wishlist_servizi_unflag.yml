langcode: it
status: true
dependencies:
  config:
    - flag.flag.wishlist_servizi
  module:
    - flag
id: flag_action.wishlist_servizi_unflag
label: Rimuovi
type: node
plugin: 'flag_action:wishlist_servizi_unflag'
configuration:
  flag_id: wishlist_servizi
  flag_action: unflag
